package com.example.moobie.Function;

import androidx.appcompat.app.AppCompatActivity;

public class MyPlugin extends AppCompatActivity {


    /*FANCYTOAST FULL SETTING ==================
      new StyleableToast
                        .Builder(context)
                        .text("Hello world!")
                        .textColor(getResources().getColor(R.color.colorWhite))
                        .backgroundColor(getResources().getColor(R.color.colorBlack))
                        .length(Toast.LENGTH_LONG)
                        .show();
                        /*.text(toastMsg)
                        .stroke(2, Color.BLACK)
                        .backgroundColor(Color.WHITE)
                        .solidBackground()
                        .textColor(Color.RED)
                        .textBold()
                        .font(R.font.dosis)
                        .iconStart(getIcon())
                        .iconEnd(getIcon())
                        .cornerRadius(12)
                        .textSize(18)*/
}
